---
description: Build, Sync, and Deploy the application to Firebase
---

This workflow ensures that all local changes are built into production assets, committed to the repository, and deployed to Firebase Hosting and Functions.

// turbo
1. Run the unified sync and deploy command:
```bash
npm run sync-deploy
```

2. Verify the live site at https://persona-recruit-new.web.app
